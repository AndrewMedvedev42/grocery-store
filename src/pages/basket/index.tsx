import { Base, Section } from "assets/styles/styled-components";

export const BasketPage = () => {
    return (
        <Base>
            <Section>
                Basket Page
            </Section>
        </Base>
    )
}